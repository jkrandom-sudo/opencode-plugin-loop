---
description: 定时重复执行 prompt。可选间隔: s/m/h/d。子命令: list | status | cancel <id> | pause <id> | resume <id> | stop-all（加 --all 跨 session）
argument-hint: "[5m] [prompt text... | list | cancel <id> | pause <id> | resume <id> | stop-all] [--all]"
agent: build
---

$ARGUMENTS